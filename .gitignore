.DS_Store
node_modules
dist/
dist-electron/
.vite/
coverage/
*.log
.qoder/
.peer-tmp/

target/

# Local CLI release staging (produced by scripts/package-cli-archive.mjs)
cli-stage/
cli-dist/

.env
.env.*
!.env.example

# 本机工作区路径与工件（不要提交）
# example 模板是仓库资产，必须用否定规则保住，否则会被上面的家族通配一起忽略。
.peer-workspace.local.json
.peer-workspace.local.*
!.peer-workspace.local.example.json

# 设计文档（本地保留，不提交）
docs/architecture/*

# 目标工作树模型配置（本地保留）
.goal-worktree-model-config/

# 误生成的证书文件
-

# 本地调试/原型目录与根目录临时脚本（不进仓库）
scratch/
demo/
_eval_gate.mjs

# QA 工件（截图/验收记录，仅在本地产生）
.peer-agent-qa/
.peer-agent-real-file-artifact.md
.peer-agent-artifact-acceptance.md
