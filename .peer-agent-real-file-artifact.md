# 真实文件产物验收

此文件由当前 Goal Runner 的 `verify-file-artifact` 子任务通过真实 `write_file` 创建。

验收目标：

- EvidenceIndex 记录 `kind: file` 的结构化用户产物；
- GoalTask 通过 EvidenceRef 关联该文件；
- TaskOverview 将其投影为可打开的文件主要产物；
- Shell 输出不进入主要产物。

生成时间：2026-08-16
