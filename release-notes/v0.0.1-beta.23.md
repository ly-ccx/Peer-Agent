## 新功能

- **计划批准即自动执行**：Plan 模式下点击「批准并执行」后，计划会自动连续推进直到完成，无需再手动逐步驱动；执行全程复用 Goal 模式的完整护栏（续推上下文注入、防偏航 re-anchor、完成验证门）。

## 优化

- **Explorer 并行调查提速**：同一轮内的多个 Explorer 子调查改为并发池执行（默认并发 5、上限 8），移除累计上限限制，单个 Explorer 失败不再中断整轮；底部进度显示「本轮已完成/本轮总数」。
