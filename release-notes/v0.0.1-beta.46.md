locale:zh-CN

## 说明

- 本版在 **0.0.1-beta.45** 之上，聚焦 **打开 App 即检测完全磁盘访问**、启动白屏修复，以及拖拽品牌 LOGO 授权体验。
- 若你仍在用 beta.45：权限只在「导入站点会话」时出现，或冷启动偶发 React #310 白屏，请直接升级本版。

## 新功能

- **启动权限门**：主壳就绪后自动 preflight 完全磁盘访问；缺权限即弹出可拖拽品牌 LOGO 的授权引导，无需先点导入。

## 修复

- **React #310 白屏**：`TokenUsageDisplay` 在 `hasInfo` early return 前固定 hooks 数量，避免启动后永久白屏。
- **启动门稳定性**：权限门改 portal 渲染，去掉易踩坑的 Overlay render-prop 路径；关闭 24h dismiss 静默。

## CLI（仅命令行）

- 无 CLI 专项变更。

locale:en-US

## Notes

- Built on **0.0.1-beta.45**. Focus: **Full Disk Access check on app launch**, cold-start white-screen fix, and brand-logo drag grant UX.
- If you are still on beta.45 and only see the permission UI after “Import site session”, or hit React #310 white screens, upgrade to this build.

## What's New

- **Startup Full Disk Access gate**: preflight after main shell is ready; missing access shows a draggable Peer logo grant surface without opening the import wizard first.

## Fixes

- **React #310 white screen**: keep TokenUsageDisplay hooks before the `hasInfo` early return so hook count never changes across startup renders.
- **Startup gate stability**: portal-based gate (no Overlay render-prop); no 24h silent dismiss.

## CLI (command-line only)

- No CLI-specific changes.
