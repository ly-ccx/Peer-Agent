## 修复

- **macOS 打包失败**：修复 `app.asar` 误排除 `@peer-agent/system-context` 运行时入口（`src/index.mjs`），使 `Verify packaged runtime entries` 通过。

## 说明

- 本版为 **0.0.1-beta.42** 的打包修复热修。
- 产品功能与 beta.42 一致；若你未成功装上 beta.42，请直接使用本版。

## CLI / npm

- 本版 GitHub Release 继续附带 CLI 归档（与 Desktop 同 tag）：`peer-darwin-arm64.tar.gz`。
- 可安装：`npm i -g @peer-agent/cli@beta`。
- 安装后执行 `peer --version` 应输出 `peer 0.0.1-beta.43`。
- 会话与设置仍落在 `~/.peer-agent`，与 Desktop 共享。
