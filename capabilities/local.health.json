{
  "capabilityId": "local.health",
  "name": "Local health check",
  "description": "Checks whether the local capability core is available without reading local files.",
  "localizedName": {
    "zh-CN": "本地 health 检查",
    "en-US": "Local health check"
  },
  "localizedDescription": {
    "zh-CN": "检查本地能力核心是否可用，不读取本地文件。",
    "en-US": "Checks whether the local capability core is available without reading local files."
  },
  "source": "native",
  "riskLevel": "L0_inert",
  "dataLevel": "D0_public",
  "health": "available",
  "inputSchema": {
    "type": "object",
    "properties": {
      "scope": {
        "type": "string",
        "enum": ["client_session"],
        "description": "Only the current client session can be checked in the first scaffold."
      },
      "workspace": {
        "type": "string",
        "description": "Display label for the current workspace."
      }
    },
    "required": ["scope"]
  },
  "evidencePolicy": {
    "returnMode": "summary",
    "maxChars": 1200,
    "redactSensitive": true
  }
}
